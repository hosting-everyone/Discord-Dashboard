import {
    DataType as TextInputDataType,
    TextInput,
} from "@/dashboard/form_options/types/textinput";

export const FormOptions = {
    TextInput,
};

export interface FormOptionsTypes {
    TextInput: TextInputDataType;
}
